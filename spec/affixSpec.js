import {Affix} from "../src/main";

describe("Affix", () => {

  it("is defined", () => {

    expect(Affix).not.toBeUndefined();
  });

  it("has top() method", () => {

    expect(Affix.top).not.toBeUndefined();
  });
});
